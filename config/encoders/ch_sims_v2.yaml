defaults:
  - _self_

audio:
  transformer:
    num_hidden_layers: 8
    num_attention_heads: 2
    intermediate_size: 1024  # MLP dim

vision:
  transformer:
    num_hidden_layers: 4
    num_attention_heads: 4
    intermediate_size: 512  # MLP dim
  
monitor:
  metric: "val/acc_5_max"
  mode: "max"